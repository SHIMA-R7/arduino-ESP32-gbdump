# N64 Transfer Pak 経由 GB カートリッジ吸い出し 開発レポート

対象リポジトリ: [SHIMA-R7/arduino-ESP32-gbdump](https://github.com/SHIMA-R7/arduino-ESP32-gbdump)
（[fl4shk/arduinogbdump](https://github.com/fl4shk/arduinogbdump) からのフォーク）

---

## 1. 概要

N64 コントローラーの拡張スロットに挿した Transfer Pak を経由して、ゲームボーイのカートリッジから ROM を吸い出すプロジェクトである。

当初の目的は「既存の Arduino 向けプロジェクトを ESP32-C6 に移植する」ことだった。しかし作業の過程で、**元プロジェクトが Transfer Pak との通信を確立できない根本的な欠陥を抱えていた**ことが判明した。最終的にはその欠陥の修正、Arduino Uno 対応、MBC3 対応、ESP32-C6 移植、さらに吸い出したソフトを遊ぶための入力ツールまでを含むフォークとして公開した。

**最終成果**

| 項目 | 結果 |
|---|---|
| 吸い出した ROM | 6 本（全てグローバルチェックサム一致） |
| 対応マッパー | ROM only / MBC1 / MBC3 / MBC5 |
| 動作ハードウェア | Arduino Uno、ESP32-C6 の両方 |
| 修正した元プロジェクトの不具合 | 3 件 |
| 付随ツール | 吸い出し CLI、GUI（exe）、コントローラー入力ブリッジ |

**吸い出した ROM 一覧**

| タイトル | マッパー | サイズ | グローバルチェックサム |
|---|---|---|---|
| Donkey Kong Country 2001 | MBC5 | 4 MiB | `0x71E7` ✓ |
| Pokemon Gold | MBC3 | 1 MiB | `0x8A70` ✓ |
| Super Mario Land 2 | MBC1 | 512 KiB | `0xA613` ✓ |
| Wario Land (Super Mario Land 3) | MBC1 | 512 KiB | `0xF4A5` ✓ |
| Super Mario Land | MBC1 | 64 KiB | `0x5ECF` ✓ |
| Tetris | ROM only | 32 KiB | `0x16BF` ✓ |

グローバルチェックサムは ROM 全バイトを対象とする検証値であり、これが一致することは全データが正しく読めていることの証明になる。

---

## 2. 対象プロトコルの構造

このプロジェクトが扱う通信は 3 層に分かれている。

```
[ホスト(Arduino/ESP32)]
      │  Joybus（1線式・半二重・1ビット4μs）
      ▼
[N64 コントローラー]
      │  拡張スロット（コマンド 0x02 読み / 0x03 書き）
      ▼
[Transfer Pak]
      │  カートリッジバス
      ▼
[GB カートリッジ + MBC（メモリバンクコントローラ）]
```

重要なのは、**各層が独立して壊れうる**という点である。この事実が後述する数々の落とし穴の根源となった。

Joybus のビット表現は以下の通り。

- `0` … Low 3μs → High 1μs
- `1` … Low 1μs → High 3μs
- ストップビット … Low 1μs → High（解放）

---

## 3. 開発の経過

### 第1段階: ESP32-C6 への移植

元コードは ATmega328P（16MHz）専用で、インラインアセンブリの NOP 命令を手作業で数えることで 1μs 精度のビットバンギングを実現していた。ESP32-C6 は 160MHz と 10 倍高速だが、**この方式は移植できない**。

理由は「速度」ではなく「決定論性」にある。

| | AVR | ESP32-C6 |
|---|---|---|
| クロック | 16 MHz | 160 MHz |
| 実行環境 | ベアメタル | FreeRTOS |
| コード配置 | 内蔵フラッシュ（固定サイクル） | フラッシュ + キャッシュ |
| タイミング保証 | アセンブリで完全制御 | コンパイラ・キャッシュ依存 |

実測でも、割り込みを禁止したソフトウェアポーリングでは、本来 1μs と 3μs にはっきり分かれるはずの Low 期間が **2〜9μs にばらついた**。

そこで **RMT ペリフェラル**（本来は赤外線リモコン用。波形の生成と計測をハードウェアで行う）を用いる方式に切り替えた。これは ESP32 における同種の実装の定石でもある。

この過程で判明した RMT 固有の落とし穴:

1. **`rmt_transmit_config_t::flags.eot_level` は明示的に 1 にする必要がある**
   既定値 0 では送信終了後にラインを Low に駆動し続ける。他から見れば実在の Low パルスと区別がつかない。

2. **RX が返す `duration0` は必ずしも Low 期間ではない**
   アーム時のライン状態から交互に記録するため、`level0` を見て判定する必要がある。誤るとビットが反転する。

3. **TX と RX は別 GPIO に分ける**
   同一ピンで共用すると信頼できない波形になり、TX チャンネルの破棄が RX チャンネルを無効状態に落とすことすらある。外部でジャンパー接続して同じ信号線に繋ぐ。

4. **受信タイムアウト後は RX チャンネルをリセットする**
   エッジが一度も来ないと受信処理が永久に保留され、以降の `rmt_receive()` が全て失敗する。

5. **`mem_block_symbols` は 48 にする**
   C6 の 1 メモリブロックが 48 シンボル。64 を要求すると 2 ブロック占有し、他がチャンネルを取れなくなる（後述）。

### 第2段階: 通信は成立するが、データが返らない

Joybus 層の実装後、コントローラーの identify コマンド（`0x00`）には正しい応答（`0x05 0x00 0x01`）が返るようになった。送信バイト列も生波形からビット単位で照合し、CRC 計算を含めて完全に正しいことを確認した。

**しかし Transfer Pak への読み書きは、何を送っても常にオール 0 を返した。**

ここから長い切り分けが続いた。

| 検証項目 | 結果 |
|---|---|
| GND / 3.3V / プルアップ抵抗 | すべて正常 |
| 送信コマンドのバイト列（CRC 含む） | 生波形で完全一致を確認 |
| 受信デコードロジック | identify で実データ取得済み |
| カートリッジを別の物に交換 | 3 種類すべて同じ結果 |
| Transfer Pak を別の物に交換 | 2 個とも同じ結果 |
| Transfer Pak を完全に外す | **結果が変わらない** |
| コントローラーのボタン・スティック | **完全に正常動作** |
| Rumble Pak での振動テスト | 反応なし |

ここで筆者（Claude）は「コントローラーのパックスロット回路の物理故障」と結論した。しかし依頼者から **「この堅牢な回路が故障しているとは思えない。プロトコルが本当に正しいのか調べ直せ」** という指摘を受けた。

これが転換点となった。

### 第3段階: 仕様書に書かれていた答え

[N64brew Wiki の Joybus Protocol](https://n64brew.dev/wiki/Joybus_Protocol) を精読したところ、次の記述があった。

> パックが挿入された直後は、読み書きは無効化されたまま（パックが無いのと同じ扱い）になる。ソフトウェア側が Info（identify）コマンドを一度送ってパックの存在を認識するまで、コントローラーは読み書きを許可しない。この最初のリクエストでステータスバイトは「3」（新規挿入）になる。

identify 応答のステータスバイト下位 2 ビットの意味:

| 値 | 意味 |
|---|---|
| `1` | アクセサリが挿入されている |
| `2` | アクセサリ無し |
| `3` | 前回の Info コマンド以降に**新規挿入**された |

元コードは `setup()` で identify を **一度だけ** 送っていた。Arduino の電源投入前からパックが挿さっていれば、この「新規挿入」の遷移を捉えられず、以降すべての読み書きが永久に無効化される。

実機で identify を連続ポーリングしながらパックを抜き差ししたところ、`2 → 3 → 1` の遷移が明確に観測され、**`3` を跨いだ直後だけカートリッジが読める**ことが確認できた。

なお、実機の N64 では電源投入時に OS が自動的にコントローラーをポーリングするため、この遷移は自然に発生する。ユーザーが抜き差しを意識する必要はない。今回それが必要だったのは、デバッグ中に何十回も identify を送ってしまい、コントローラー内部の「このパックは既知」という状態を消費し尽くしていたためである。

**試したが代用にならなかった手法**: 2N2907（PNP トランジスタ）でハイサイドスイッチを組み、3.3V 線を遮断してパックを電源断→再投入する方法を試した。電源断中は identify が無応答になることから遮断自体は成功していたが、復帰後のステータスは `3` ではなく `1` だった。「新規挿入」は電源投入ではなく**物理的な挿入動作そのもの**を検出している。

### 第4段階: 「正しく見える誤ったダンプ」との戦い

アンロック手順の実装後、ROM の吸い出しに成功した。しかしここから、**検証を怠れば気付かずに壊れたデータを掴む**という別種の問題が連続して現れた。

#### 4-1. バンク0の複製（Super Mario Land 2）

1 回目のダンプは、ヘッダもニンテンドーロゴもヘッダチェックサムも正常だった。しかし**グローバルチェックサムだけが不一致**。調査したところ、512KB 全体が**バンク 0 の 32 回コピー**だった。

原因は「読み出しは成功するが、書き込み（バンク切り替え）だけが効かない」状態。読み書きは独立して失敗しうる。

対策として、挿入ごとに**書き込みが実際に効くか**を検証するゲートを追加した。

#### 4-2. データバスのビット固着（Wario Land）

ヘッダが `07 04 06` と読め、カートタイプ `0x07` という GB 規格に存在しない値になった。しかも **6 回読んでも完全に同じ値**という紛らわしさだった。

正常時の値と比較すると:

```
不良: 4E 44 37 04 04 04 04 07 04 06 04 05 04 ...
正常: 4E 44 33 00 00 00 00 03 04 02 00 01 00 ...
```

**全ての差分がちょうど `0x04`** — データバスのビット 2 が固着していた。

さらに厄介なのは、検証に使っていたニンテンドーロゴ `CE ED 66 66` は **4 バイトとも元々ビット 2 が立っている**ため、この故障を素通りさせてしまう点だった。

対策として、ヘッダチェックサム（GB 0x014D）の照合を追加した。

#### 4-3. カートリッジ側マッパーの固着（Donkey Kong Country 2001）

4MB の ROM が、実在する多様なデータでありながらヘッダを含まない、という状態でダンプされた。しかも**3 回連続で完全に同一の結果**という再現性を持っていた。

解析の結果:

- 各 32 バイトブロックがきっちり **256 回ずつ**出現 → 同じ 16KB 窓を 256 回読んでいる
- ただしバンク 0 内には 506 種類の異なるブロック → ゴミではなく実データ

つまり**カートリッジが電源投入時の既定バンク（バンク1）のまま固定**され、そこを 256 回読んでいた。

ここで重要なのは、**Transfer Pak 自身のバンク切り替えは正常に動作していた**という点である。既存の検証（4-1 で追加したもの）は Transfer Pak のバンクレジスタしか見ておらず、その先にあるカートリッジの MBC が固まっている状態を検出できなかった。

対策として、**カートリッジを実際にバンク 1 と 2 で切り替え、内容が変化することを確認する**検証を追加した。この検証は導入直後の実行で早速 1 回目の挿入を弾き、2 回目で正常なダンプを得られた。

#### まとめ: 現在の検証ゲート

挿入ごとに以下をすべて確認し、一つでも失敗すれば再挿入を促す。

| # | 検証内容 | 捕捉できる故障 |
|---|---|---|
| 1 | ニンテンドーロゴ（GB 0x0104） | 読み出し全般の失敗 |
| 2 | ヘッダチェックサム（GB 0x014D） | データバスのビット固着 |
| 3 | Transfer Pak のバンク切り替え | パックへの書き込み不能 |
| 4 | カートリッジ MBC のバンク切り替え | マッパーの固着 |

さらにホスト側で**グローバルチェックサム**を照合し、全バイトの正当性を最終確認する。

### 第5段階: 接触不良という現実

以上の検証を通過する挿入は、**1 回目で通ることもあれば十数回かかることもある**。パックスロットの接触が経年で不安定になっているためである。

そこで「成功するまで抜き差しを促し続ける」リトライループを実装し、LED で状態を示すようにした。

| LED | 意味 |
|---|---|
| 点滅 | 抜き差ししてください |
| 点灯（ESP32 は緑） | 検証通過、触らないでください |
| 赤（ESP32 のみ） | この挿入は失敗、続けて抜き差し |

これにより、シリアルログを睨む必要がなくなり、実用的な作業手順に落ち着いた。

### 第6段階: RGB LED と RMT チャンネルの競合

ESP32-C6 の LED 対応時、Joybus 通信が全面的に失敗するようになった（`no free tx channels`）。

原因は **`neopixelWrite()` が RMT ペリフェラルを使う**ことにあった。C6 の RMT は 1 チャンネルあたり 48 シンボルであり、64 シンボルを要求していた Joybus のチャンネルは 2 ブロックを占有していた。そこに LED が 1 チャンネル取ると枯渇する。

`mem_block_symbols` を 48 に下げることで共存が可能になった。結果として、無駄なメモリ要求を削るという副次的な改善にもなった。

### 第7段階: 吸い出したソフトを遊ぶ

吸い出した ROM を、吸い出しに使った同じ N64 コントローラーで遊べるようにした。Joybus 層を逆向きに使い、コントローラーの状態をホストに送ってキーボード入力に変換する。

ここでも一つ罠があった。**キー入力が RetroArch にまったく届かない**という症状で、入力ドライバ（DirectInput）の問題や権限（UIPI）を疑って調査したが、実際は自作コードのバグだった。

64bit Windows において `INPUT` 構造体は **40 バイト**でなければならない。共用体のサイズは最大メンバである `MOUSEINPUT`（32 バイト）で決まるが、`KEYBDINPUT`（24 バイト）基準で宣言していたため 32 バイトになっていた。この場合 `SendInput` は `ERROR_INVALID_PARAMETER` で失敗するが、**戻り値を確認していなかったため沈黙のまま何も起きなかった**。

外から見ると入力ドライバや権限の問題と区別がつかない症状であり、戻り値検査の欠如が診断を大きく遠回りさせた。現在は戻り値を必ず検査し、起動時に構造体サイズを表明している。

---

## 4. 元プロジェクトへの修正

| 修正内容 | 影響 |
|---|---|
| Transfer Pak アンロック手順の実装 | **これが無いと一切動作しない** |
| `mbc3_dump_rom()` の実装 | 宣言のみで未定義だったため、MBC3 カート（ポケモン金銀等）は無反応だった |
| `bufsize` を 1072 → 300 に削減 | Uno の 2KB SRAM に収まらずビルド不能だった（README に「Uno 対応は予定」とあった件を解決） |

---

## 5. 成果物

### ファームウェア

- `src/arduinogbdump/` — Arduino AVR 版（Uno で動作確認）
- `src/esp32c6_gbdump/` — ESP32-C6 版（PlatformIO / Arduino フレームワーク）

ESP32-C6 版はワリオランドを吸い出し、Uno 版が吸い出した同じカートリッジと **MD5 完全一致**（`d9d957771484ef846d4e8d241f6f2815`）を確認済み。

### ツール

- `tools/dump.py` — CLI 吸い出しツール。検証に失敗すると非ゼロ終了する
- `tools/gbdump_gui.py` — GUI（Tkinter）。exe 化可能
- `src/n64_keyboard/` — コントローラー入力ブリッジ

### 配線

**Arduino Uno**

```
ピン2 ─┬─ N64 データ線
       └─ 1kΩ ─ 3.3V
GND ──── N64 GND
3.3V ─── N64 3.3V
```

ピン 2 は「入力（プルアップで 3.3V）」か「出力 LOW」の 2 状態しか使わず、**5V を出力しない**。これは N64 側が 3.3V 系であるため必須の設計である。

**ESP32-C6**

```
GPIO0 ─┬─ N64 データ線
GPIO1 ─┤   （ジャンパーで GPIO0 と接続）
       └─ 1kΩ ─ 3.3V
GND ──── N64 GND
3.3V ─── N64 3.3V
```

ESP32 は 3.3V 系だが、プルアップ抵抗は省略できない。電圧変換のためではなく、**エッジの立ち上がり速度を確保するため**である。内蔵プルアップ（数十kΩ）では 1μs のパルスに追従できない。

N64 コネクタのピン配置（平らな面を下）: 左 = GND、中央 = データ、右 = 3.3V

---

## 6. 得られた知見

### 技術面

**「速い」と「正確」は別である。** AVR は 16MHz と低速だが、ベアメタルでキャッシュも OS もないため、アセンブリのサイクル数で完全にタイミングを制御できる。ESP32 は 10 倍高速だが、キャッシュ・OS・抽象化レイヤの存在により μs 単位の決定論的タイミングは保証されない。適切な解はハードウェアペリフェラルへの委譲だった。

**沈黙する失敗が最も高くつく。** 本プロジェクトで最も時間を要した 2 つの問題は、どちらも「エラーを出さずに黙って何もしない」ものだった。`SendInput` の戻り値未検査と、Transfer Pak がオール 0 を返す仕様である。戻り値の検査と、期待値との照合による能動的な検証がなければ、症状から原因への距離は著しく遠くなる。

**多層構造では各層を個別に検証する。** 読み出しと書き込み、Transfer Pak のバンクとカートリッジのバンク — これらはすべて独立に失敗しうる。ある層の正常動作は、その先の層の正常動作を意味しない。最終的に 4 段階の検証ゲートに至ったのは、この教訓を一つずつ痛みとともに学んだ結果である。

**チェックサムは「正しく見える誤り」に対する唯一の防御である。** ヘッダが読め、ロゴが一致し、データに多様性があっても、なお完全に誤ったダンプは存在しうる。全バイトを対象とするグローバルチェックサムだけが、それを機械的に検出できた。

### プロセス面

本プロジェクトの決定的な転換点は、筆者が「ハードウェアの物理故障」と結論した場面で、**依頼者がその結論を受け入れず、仕様の再確認を求めた**ことだった。

その時点で筆者が挙げた根拠は「自分が書いたコードにバグが見つからない」ことであり、これは「したがってハードウェアが壊れている」ことの証明にはなっていなかった。実際には、参照していた元実装そのものが仕様を満たしていなかった。

自分の調査範囲の内側だけを見て結論を出すと、範囲の外にある前提の誤りは永遠に見つからない。押し返してもらえたことで、初めて仕様書に立ち返ることができた。

---

## 7. コミット履歴

| コミット | 内容 |
|---|---|
| `b75a607` | カートリッジ側マッパーの検証を追加 |
| `7c80f9c` | ESP32-C6 版が実機で完動、Uno 版と一致を確認 |
| `4e4a137` | コントローラー入力ブリッジを追加 |
| `f8303f4` | ヘッダチェックサム検証を追加 |
| `1da57b0` | Transfer Pak アンロック修正、Uno 対応、MBC3 対応、ESP32-C6 移植 |
| `cc6e9fa` | （fl4shk 氏のオリジナル） |

各コミットメッセージには、成功した内容だけでなく**試して駄目だった手法**も記録している。後から同じ道を辿る者が、同じ失敗を繰り返さずに済むようにするためである。
